name: Build

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: wetransform/gha-workflows/.github/workflows/dockerfile-build.yml@733faab9b81e9c64de96ae12a251b3b76111a4eb # v3.1.0
    with:
      image: wetransform/postfix
      tag: pr
