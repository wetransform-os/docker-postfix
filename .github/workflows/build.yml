name: Build

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: wetransform/gha-workflows/.github/workflows/dockerfile-build.yml@a78448bc4e8511a19e0362cfc0c450d76ca68b7f # v2.1.0
    with:
      image: wetransform/postfix
      tag: pr
