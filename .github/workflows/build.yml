name: Build

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: wetransform/gha-workflows/.github/workflows/dockerfile-build.yml@6cf8743e1cdaff405473951cc6ad4eadaf2226cc # v4.1.2
    with:
      image: wetransform/postfix
      tag: pr
