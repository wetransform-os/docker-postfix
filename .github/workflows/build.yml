name: Build

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: wetransform/gha-workflows/.github/workflows/dockerfile-build.yml@7f8cab53bf0b02b6400307f8e3a9fb68dd648902 # v3.8.0
    with:
      image: wetransform/postfix
      tag: pr
